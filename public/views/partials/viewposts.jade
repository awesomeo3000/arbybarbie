.view-container
	.view-post(ng-repeat='post in posts')
		.view-heading.text-center
			h2
				| {{post.title}}
		.view-author
			| author: {{post.author}}
		.view-content
			| {{post.content}}
button(ng-click='getPosts()')
	| Click me